# --- Réservation ---
PORT_RESERVATION=8080
THREAD_POOL_SIZE=10

# --- Base de données ---
DB_HOST=127.0.0.1
DB_USER=user
DB_PASSWORD=pass
DB_NAME=reservations

# --- Admin & timeouts ---
PORT_ADMIN=8081
CONNECTION_TIMEOUT=300
